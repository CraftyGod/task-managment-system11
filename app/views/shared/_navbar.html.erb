<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/tasks">Tasks</a>
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item">
          <span class="border border-dark ml-2"><%= current_user.firstName + ' ' + current_user.lastName %></span>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'btn btn-outline-dark ml-2' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Register', new_user_registration_path, class: 'btn btn-outline-dark ml-2' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-dark ml-2' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
